@{
    ViewBag.Title = "Home Page";
    Layout = "_Layout";

    var dt = ViewBag.DT;
    string selected = "";
}
@model stu

<div>
    <h3>選取</h3>
    <form method="post" action="/transch/Home/generate">
    <select asp-for="stuno">
    @for (int i = 0; i < dt.Rows.Count; i++)
    {
            <option value="@Html.Raw(dt.Rows[i]["stuno"])">@Html.Raw(dt.Rows[i]["name"])</option>
    }
    </select>
    <input type="submit" />
    </form>
    <p>只有林聖博有填值，選取林聖博即可生成成績單，如要測試可自行新增學生和新增修課成績後就能生成成績單，此為校務系統開發英文成績單之簡易中文測試</p>
</div>